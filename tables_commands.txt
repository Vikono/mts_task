# cur.execute("CREATE TABLE info(id INTEGER PRIMARY KEY, alias TEXT, title TEXT, description TEXT, cardImageUrl TEXT, \
    #              priority INTEGER, marketingId INTEGER, isPriorityProduct INTEGER, categoryOrder INTEGER, \
    #              tariffType TEXT, useRequestConnectionForm INTEGER)")
    # con.commit()
    #
    # cur.execute("CREATE TABLE characteristics(id INTEGER, title TEXT, value TEXT, numValue TEXT, \
    #              displayUnit TEXT, quotaUnit TEXT, isUnlimited INTEGER, numValueType, baseParameter, \
    #              PRIMARY KEY(id, baseParameter))")
    # cur.execute(
    #     "CREATE TABLE productLabels(id INTEGER, text TEXT, type TEXT, sortOrder INTEGER, PRIMARY KEY(id, text))")
    # cur.execute(
    #     "CREATE TABLE productFeatures(id INTEGER, title TEXT, value, isUnlimited, numValueType, \
    #     iconUrl, baseParameter, PRIMARY KEY (id , title))")
    # cur.execute("CREATE TABLE benefitsDescriptionDescr(id INTEGER PRIMARY KEY, descr TEXT)")
    # cur.execute("CREATE TABLE benefitsDescriptionIcons(id INTEGER, icon TEXT, PRIMARY KEY (id, icon))")
    # cur.execute("CREATE TABLE subscriptionFee(id INTEGER, title TEXT, value TEXT, numValue INTEGER, \
    #              displayUnit TEXT, quotaUnit TEXT, quotaPeriod TEXT, isUnlimited INTEGER, numValueType TEXT, \
    #              sortOrder INTEGER, baseParameter TEXT, PRIMARY KEY (id, title))")
    # cur.execute("CREATE TABLE regionalCodes(id INTEGER, code TEXT, PRIMARY KEY (id, code))")
    # cur.execute("CREATE TABLE btnflags(id INTEGER PRIMARY KEY, isConnectButtonHidden INTEGER, \
    #             isActionButtonHidden INTEGER, isDetailsButtonHidden INTEGER)")



                for each in tariff['allRegionalGlobalCodes']:
                data = [tariff['id'], each]
                print(type(each))
                cur.execute(f"INSERT INTO regionalCodes VALUES(?, ?)", data)

                data = [tariff['id'], tariff['isConnectButtonHidden'],
                    tariff['isActionButtonHidden'], tariff['isDetailsButtonHidden']]
            cur.execute(f"INSERT INTO btnflags VALUES(?, ?, ?, ?)", data)